# METAPAT stack repair — completed looks like

The METAPAT repair is complete only when every applicable statement below is true and evidenced by code, tests, generated metadata, or current documentation.

## Authority and architecture

- **Completed looks like:** METAPAT is named as the authority for Meta Energy Theory canon, semantic constraints, and claim boundaries.
- **Completed looks like:** runtime evidence flow is documented separately from authority flow.
- **Completed looks like:** no document still implies that UCNS owns METAPAT ontology or that EDCM measurements establish METAPAT root validity.
- **Completed looks like:** `README.md`, `ROADMAP.md`, `flow_plan.py`, `AGENTS.md`, `llms.txt`, and package metadata describe the same architecture.

## UCNS boundary

- **Completed looks like:** METAPAT contains no independent class or operation presented as a local UCNS algebra.
- **Completed looks like:** the live adapter uses the actual `ucns` public package, or METAPAT emits a clearly neutral versioned transport record for an external adapter.
- **Completed looks like:** missing UCNS support produces a precise dependency error instead of a silent local fallback.
- **Completed looks like:** a round-trip integration test demonstrates that METAPAT provenance survives conversion to and from an actual UCNS object.
- **Completed looks like:** the adapter does not import or imply UCNS theorem status.

## Claims and executable checks

- **Completed looks like:** `docs/claims-ledger.md` classifies every public claim as root stipulation, definition, internal derivation, implemented contract, cross-domain hypothesis, empirical frontier, or retracted/superseded.
- **Completed looks like:** boolean helper functions are documented as canon contract checks, not independent theorem proofs.
- **Completed looks like:** every contract test says what it checks and what it cannot establish.
- **Completed looks like:** Chapter Zero canon remains exact unless a separately authorized canon change exists.

## EDCM boundary

- **Completed looks like:** METAPAT exports a versioned immutable semantic module envelope with canon identity, source references, permitted interpretations, provenance, and unresolved constraints.
- **Completed looks like:** the envelope contains no computed EDCM metric values.
- **Completed looks like:** an EDCM integration fixture consumes the envelope while preserving the distinction between semantic authority and measured readout.
- **Completed looks like:** unresolved `hmmm` content survives serialization and round trip.

## Packaging and tests

- **Completed looks like:** tests import `metapat`, not `src.metapat`.
- **Completed looks like:** the package builds into a wheel and sdist, passes `twine check`, installs in a clean environment, and exposes one authoritative version.
- **Completed looks like:** optional integration extras in `pyproject.toml` are real and testable rather than empty placeholders.
- **Completed looks like:** unit tests, package tests, adapter tests, and the shared UCNS/METAPAT/EDCM contract suite pass from a clean checkout.
- **Completed looks like:** skipped sibling-repository integration tests are explicit and visible; no test silently substitutes a fake implementation.

## Agent and skill-lib compliance

- **Completed looks like:** every changed executable source has accurate msdmd metadata.
- **Completed looks like:** repo-local skill-lib drift checks report clean against the declared canonical commit.
- **Completed looks like:** no agent-facing instruction points to the retired local UCNS bridge as authoritative.

## Final boundary

- **Completed looks like:** a coding specialist can answer, from repository evidence alone, all four questions below without guessing:

  1. What does METAPAT own?
  2. What does UCNS own?
  3. What may EDCM measure?
  4. Which claims remain unresolved?

## hmmm

Completion does not require deciding whether METAPAT source statements become UCNS payloads, tags, or external provenance references. Completion requires that the unresolved choice be represented honestly, versioned, tested for preservation, and prevented from becoming accidental semantics.