# Event/trace lift candidate v0

**Standing:** stack-local post-observation research. Not UCNS canon, not a
recovered recursive constructor, and not a PCEA or cryptographic claim.

## Result

The pinned Mobius Seed geometry supplies two exact, independently executable
counts:

```text
pairwise projection events                         = 39
center-rooted seven-band construction orders      = 720 = 6!
source-declared boundary multiplicity per pair    = 4
Structural Null origins                           = 1
```

Applying the candidate boundary lift

```text
L(x) = 1 + 4*x
```

produces two exact identities:

```text
L(39)  = 157
L(720) = 2881
```

This is an exact post-observation, structure-derived accounting candidate for
the first two observations. It is not a recovered recursive law. The first
identity uses an event count and the second uses a construction-trace count;
UCNS has not defined one unchanged state extractor that turns the former into
the latter. The four boundary events are source-declared per structural
relation, not selected as one global cardinality multiplier.

Because this relationship was found after all three values were known, both
matches are **retrodictive only**. They cannot be labeled `SURVIVED`.

## Source boundary

The executable experiment binds the current workspace base:

```text
UCNS BASE.json commit = 1975fe70cf4e0826a8020c2da3047569e277af64
Public Gonol digest   = 55d10c84529a4d7bc7714786357e977b68d9df2ac3f73d20e229580b552c2ef5
```

The local `origin/main` Git object is newer than the checked-out stack and
contains `mobius_seed_construction.py` under stack commit
`9d53a9ce8393229af857bdef4539709c649d1577`, pinning UCNS
`828c0b8bbcfc267efb5701da714191c1f73a81ff`. Its construction rule is the same
center-rooted structural-vesica adjacency used here. It is corroborating local
disk evidence only; this experiment does not silently refresh `libs/` or
`BASE.json`.

## Exact construction count

The structural relation ledger is the wheel graph on seven band slots:

```text
six CENTER--RING edges
six adjacent RING--RING edges
```

Construction begins with `CENTER`. Every ring slot is immediately buildable
because it has a structural relation to `CENTER`. The six identified ring
slots can therefore be built in any permutation:

```text
complete construction traces = 6! = 720
```

The executable implementation also counts this by subset dynamic programming
from the graph itself; it does not place `720`, `2881`, or the third observed
value inside the counting operation.

## Second gate

The third observation has the same arithmetic lift shape:

```text
(54837698421 - 1) / 4 = 13709424605
```

No current UCNS object identifies `13709424605` as the event, trace, closure,
or incidence count of the completed 2881-gonol. Three bounded continuations
were run with the exact same adjacency-order counter and `L(x)` lift:

| candidate | vertices | edges | construction traces | lifted cardinality | result |
|---|---:|---:|---:|---:|---|
| complete radius-two hex disk | 19 | 42 | 97934946851520 | 391739787406081 | `FALSIFIED` |
| structural-relation incidence | 19 | 24 | 3625881589440 | 14503526357761 | `FALSIFIED` |
| projection-node incidence | 20 | 36 | 648016379781120 | 2592065519124481 | `FALSIFIED` |

The radius-two geometry is a bounded research alternative, not selected UCNS
authority. Its failure is not permission to tune a partial ring toward the
target. The incidence alternatives likewise promote only already-declared
seed objects and add no constants or target branches.

```text
RETRODICTIVE_IDENTITIES = 2
FALSIFIED_CONTINUATIONS = 3
CONSTRUCTOR_SURVIVORS   = 0
NEXT_PREDICTION         = hmmm
```

## Prior constructor audit

`composed_successor_constructor.py` computes `4*n + 1` by summing input
participants, participation records, closed affinizations, promoted atoms, and
one closure whole. Those are lifecycle artifacts for the same occurrences,
not five disjoint classes of successor positions. The frozen implementation
still deterministically reports `157 -> 629`, but the mechanics do not
authorize that ledger sum as geometric cardinality. Its first gate remains
`FALSIFIED`; `629` should not be treated as a competing gonol geometry.

## Replay

```bash
PYTHONDONTWRITEBYTECODE=1 python3 research/ucns/event_trace_lift_candidate.py
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest research/ucns/tests/test_event_trace_lift_candidate.py -v
```

Frozen producer identities after the 2026-09-13 canonical snapshot refresh:

Only source hashes and derived receipt identities changed; the research results,
standing, nonclaims, and unresolved boundaries are identical. The historical
research BASE remains fixed. The original receipt is recoverable at Stack
`85d6839` before this refresh; see the [refresh record](../../../docs/updates/2026-09-13-tools.md).

```text
event_trace_lift_candidate.py sha256 = d1cb1e301de8d081653c0cc8f07730ae09afac652867236c266c3c9b67d7b9b4
test module sha256                   = 986ef143e271acad8560e0c0fa1381a92ea05a219d8b2770ab79e4a361026dfd
canonical receipt payload sha256     = fa46cc04ce7a71300050a898403d9769e7242027f3920894851cd6605c80e392
formatted receipt file sha256        = 9694627a14441c3377e7987b9b369664422e9ce57f48cff678bc781ebc22f04a
```

## hmmm

- The relationship `1 + 4*x` is now an exact structural candidate, but UCNS
  has not selected the per-relation boundary multiplicity as a global lift.
- The operation that promotes the 39-event ledger into the 720-trace state is
  not yet a scale-invariant executable mechanic.
- The complete 2881-gonol constituent, event, orientation, and structural
  relation ledgers remain absent.
- A legitimate second-gate test requires those ledgers. The integer
  `13709424605` is a falsification comparator, not permission to reverse-engineer
  geometry around it.
- PCEA key research remains gated. Gonol cardinality is not entropy or
  cryptographic hardness.
