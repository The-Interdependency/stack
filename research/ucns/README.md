# UCNS research workspace

Stack-local research against the pinned canonical UCNS view in [`../../libs/ucns/`](../../libs/ucns/).

Standing: **research, not canon**. Canonical UCNS authority remains `The-Interdependency/ucns` at the exact commit recorded in [`BASE.json`](BASE.json) and the root stack manifest.

## Usage guidance

1. Read `BASE.json` before beginning work.
2. Treat `../../libs/ucns/` as read-only imported canon.
3. Put experiments, candidate changes, measurements, and notes here rather than editing `libs/`.
4. If work changes UCNS itself, route the accepted change upstream; after merge, refresh `libs/ucns/` and this workspace's base commit.
5. If work becomes a distinct composed project, keep its authority separate rather than silently promoting this workspace.

## hmmm

- The [Structural Null jurisdiction and unresolved attachment record](docs/pointed-torsor-origin-attachment-canon-amendment-draft-v0.md)
  separates two decisions. The intrinsic/extrinsic jurisdictional model is
  `RATIFIED` and is proposed upstream by UCNS commit `fdc0cf9` in
  [PR #220](https://github.com/The-Interdependency/ucns/pull/220), which is
  open and unmerged. Existing behavior is `COMPATIBLE_BUT_PARTIAL`. Attachment
  remains `UNRESOLVED`: `iota` is only a research placeholder, its codomain and
  target are `hmmm`, and no pointing, residual-symmetry consequence,
  implementation, or evidence is claimed. The next comparison is restricted
  to the native visible phase basepoint and its `C2`-invariant two-lift fiber.
- The [origin-attachment basepoint symmetry audit](docs/origin-attachment-basepoint-symmetry-v0.md)
  analyzes the smallest pinned-canon traversable candidate before any canon
  ratification. With frame forgotten, the native exact root loop is the
  rational-displacement action groupoid on `Q/Z`. Its displacement-label-
  preserving automorphism group is exactly `Q/Z` acting by translations, and
  that action is simply transitive. Thus all admissible unframed base objects
  form one orbit, but no object is fixed; exact half-turn translation moves
  phase zero while preserving the entire admitted structure. Status:
  `TRANSITIVE_SYMMETRY`, with secondary classification
  `UNIQUE_ORBIT_NOT_OBJECT`. The stack-local bouquet's high-valence center is
  rejected as circular because its shared-basepoint attachment is the missing
  assumption. No `iota` is implemented and no downstream field is evaluated.
  [Frozen receipt](receipts/origin-attachment-basepoint-symmetry-v0.json).
- The [origin-attachment canon gap](docs/origin-attachment-canon-gap-v0.md)
  narrows the constructor stop to one authoritative missing relation. Public
  Gonol position zero and coordinate-free Structural Null are exact, but no
  canonical callable or certificate maps either origin representation into a
  traversable recursive object. Carrier operations preserve null as null;
  vesica, seed, and prime lifts retain positive origin clearance or nonzero
  lanes; and the package facade only co-exports otherwise separate primitives.
  The current admissible attachment set is empty. A conditional native
  phase-zero target fiber has one visible class and two unselected framed
  lifts, but those are not attachment choices. Status: `CANON_GAP` at
  `origin_attachment`; every downstream field remains unevaluated.
  [Frozen receipt](receipts/origin-attachment-canon-gap-v0.json).
- The [based-traversal constructor contract](docs/based-traversal-constructor-contract-v0.md)
  evaluates the five required fields in dependency order against pinned UCNS
  geometry and the frozen selection/provenance audits. Exact carrier-origin
  primitives are retained, but no authority-bound operation attaches that
  origin to one recursive return-groupoid object. The run therefore stops at
  the first field with `STOP_MISSING_ORIGIN_ATTACHMENT`; direction, rotation,
  outgoing dart, and closure are `NOT_EVALUATED_DEPENDENCY_BLOCKED`. The
  constructor certificate and every downstream output remain null, and PCEA
  handoff is prohibited. [Frozen receipt](receipts/based-traversal-constructor-contract-v0.json).
- The [based-traversal provenance history audit](docs/based-traversal-provenance-history-audit-v0.md)
  searched all local refs in UCNS and the exact A0 source-provenance repository,
  binding 3160 unique reachable commits, the exact ref sets, broad semantic
  candidate sets, and 21 high-signal artifacts. Position-zero origin, local
  direction helpers, two-turn return, source-bound seams, source-order codecs,
  generic traversal, prime-link marks, and post-pinned seed construction all
  fail to provide the recursive five-field certificate. Origin attachment,
  directed tangent/chirality, rotation system, marked outgoing dart, and
  closure rule remain `UNRESOLVED_NO_QUALIFYING_GEOMETRIC_SOURCE`. Status stays
  `STOP_NO_GEOMETRY_SELECTED_BASED_TRAVERSAL`; no traversal, monodromy,
  arithmetic readout, successor, or fourth gonol is emitted. The PCEA control
  `164513086777` is `UNRESOLVED`, neither survived nor falsified.
  [Frozen receipt](receipts/based-traversal-provenance-history-audit-v0.json).
- The [geometry-selected based traversal audit](docs/geometry-selected-based-traversal-audit-v0.md)
  tests the exact next gate below the ordered word layer. Public Gonol position
  zero and Structural Null are intrinsically distinguished, but Structural
  Null is coordinate-free and current geometry supplies no attachment to the
  non-null promoted groupoid base object. Native signed returns are exchanged
  by exact reflection, and the unmarked rank-three identical-loop shape has a
  full six-word `S3` orbit with no fixed once-each word. Carrier order, local
  defaults, caller slots, construction ordinals, and unmapped seed order are
  not promoted. Status: `STOP_NO_GEOMETRY_SELECTED_BASED_TRAVERSAL`; basepoint,
  orientation, traversal word, attaching word, monodromy, arithmetic readout,
  and numerical successor remain null. The missing structure is a geometric
  origin attachment plus directed rotation system, marked outgoing dart, and
  closure rule. [Frozen receipt](receipts/geometry-selected-based-traversal-audit-v0.json).
- The [ordered complete-return groupoid](docs/ordered-complete-return-groupoid-v0.md)
  is the executable layer below homology. It lifts the retained relation basis
  to a minimal subdivided-bouquet path groupoid with free-group candidate
  `F_r`, typed positive/reversed frame objects, based words, free reduction,
  and explicit later abelianization. The exact based permutation counts at
  ranks `1..4` are `1, 2, 6, 24`; oriented cyclic closure classes are
  `1, 1, 2, 6`; every rank has one once-each `H_1` vector. Thus all six
  triadic words remain distinct when based, while unbased cyclic closure has
  two oriented classes. [Frozen receipt](receipts/ordered-complete-return-groupoid-v0.json).
- The [ordered return invariant audit](docs/ordered-return-invariant-audit-v0.md)
  computes exact Fox derivatives in `Z[F_r]` and degree-two noncommutative
  Magnus expansions before abelianization. All six triadic based permutations
  have distinct Fox and Magnus fingerprints but one homology vector. Exact
  rank-two inner and rank-three commutator-shear automorphisms also act
  nontrivially on words and identically on `H_1`, proving that relation rank
  does not select global monodromy. Current geometry supplies no
  cross-generator attaching relators, crossings, or monodromy, so no
  presentation matrix, integer invariant, factor comparison, or numerical
  successor is authorized. Status:
  `ORDERED_LAYER_EXECUTABLE__MONODROMY_PRESENTATION_AND_FACTOR_BRIDGE_UNRESOLVED`.
  [Frozen receipt](receipts/ordered-return-invariant-audit-v0.json).
- The [relation-to-factor bridge obstruction audit](docs/relation-factor-bridge-obstructions-v0.md)
  proves that a stable prime label on each retained relation generator cannot
  explain the observations: nested relation bases would force divisibility and
  nested prime sets, but the three observed gonol cardinalities are pairwise
  coprime. A local trace-only map is also falsified because all return loops are
  geometrically identical and would repeat one factor as `p^2` at rank two,
  contradicting squarefreeness. Hash-to-prime and scale-to-prime rules are
  rejected as nongeometric.
- Current complete-return homology is free `Z^r`, with no torsion or finite
  order to factor. The earlier obstruction audit left a global integer
  presentation as its broad remaining class, but the ordered audit now
  refines the prerequisite: UCNS must first derive noncommutative traversal,
  attaching, and monodromy words. A determinant is only a later readout after
  that presentation and any specialization are frozen. Status remains
  `NO_SURVIVING_EXECUTABLE_FACTOR_CARDINALITY_MAP`; numerical `n4` remains
  `hmmm`. [Frozen receipt](receipts/relation-factor-bridge-obstructions-v0.json).
- The [complete-return relation extension](docs/complete-return-relation-extension-v0.md)
  is now an executable target-free geometric candidate. Exact native Mobius
  states at zero and two turns close only after complete frame restoration;
  the one-turn reversed-frame occurrence remains distinct. Their two-edge
  quotient has boundary rank one and unfilled first-homology rank one, so the
  candidate preserves prior relation ids and appends exactly one new return
  relation per recursive closure. Higher-dimensional attachment, filling
  two-cells, and monodromy remain unresolved; this is not UCNS canon.
- The separately frozen [relation-rank experiment](docs/complete-return-relation-rank-experiment-v0.md)
  applies that unchanged operation before opening arithmetic gates. Generated
  ranks `1, 2, 3` match observed `omega = 1, 2, 3` retrodictively, and one
  further application generates relation rank `4`. The result is
  `TARGET_FREE_RETRODICTIVE_RELATION_RANK_MATCH`, not out-of-sample evidence.
  No UCNS operation yet maps the return-cycle generator to an independently
  derived arithmetic-prime cardinality, so factor interpretation and numerical
  `n4` remain unresolved. Frozen receipts: [candidate](receipts/complete-return-relation-extension-v0.json)
  and [rank gate](receipts/complete-return-relation-rank-experiment-v0.json).
- The [squarefree divisor-lattice control](docs/squarefree-divisor-lattice-control-v0.md)
  freezes the exact post-observation classification
  `157: B_1`, `2881: B_2`, `54837698421: B_3` and preregisters only the
  fourth structural invariants: squarefree, `omega = 4`, `tau = 16`,
  number-theoretic `mu = +1`, divisor lattice `B_4`. Its
  [receipt](receipts/squarefree-divisor-lattice-control-v0.json) contains no
  numerical fourth value. A match would survive one out-of-sample structural
  test; it would not recover the gonol constructor.
- The executable factor-semantics audit returns
  `UNRESOLVED_NO_DECLARED_FACTOR_MAPPING`: mechanic 1 preserves the fixed
  157-position carrier, mechanic 2 takes arity from an explicit caller-supplied
  participant tuple, and mechanic 3 promotes one closed whole to one atom
  without defining the next carrier cardinality. Current UCNS mechanics do not
  yet explain why apparent Boolean divisor-lattice rank increases by one.
- The exact `39 -> 720` accounting identity remains retained as a useful
  falsified local mechanism. Its unchanged recursion produces state `258819`
  and lifted gonol `1035277`, so it does not explain the `B_1 -> B_2 -> B_3`
  progression.
- Two post-observation, structure-derived accounting identities match the
  first two observations:
  `157 = 1 + 4 * 39`, where `39` is the pinned seed's exact pairwise
  projection-event count, and `2881 = 1 + 4 * 720`, where `720 = 6!` is the
  exact count of center-first connected seed construction traces. Because both
  observations were already known, these are `RETRODICTIVE_MATCH_ONLY`, not a
  surviving constructor. See [event/trace lift candidate v0](docs/event-trace-lift-candidate-v0.md)
  and its [frozen receipt](receipts/event-trace-lift-candidate-v0.json).
- The third observation has the same arithmetic shell,
  `54837698421 = 1 + 4 * 13709424605`, but the completed `2881`-gonol ledger
  needed to derive `13709424605` does not exist. Three unchanged,
  geometry-derived continuations were tested and all were `FALSIFIED`:
  radius-two hex disk, structural-relation incidence, and projection-node
  incidence. `SURVIVORS = 0`; no next prediction is authorized.
- The earlier `157 -> 629` result is the sum of four lifecycle ledgers plus a
  closure whole (`4n + 1`). The mechanics do not establish those ledgers as
  disjoint geometric positions, so `629` remains a falsified bookkeeping
  cardinality rather than a competing gonol geometry.
- The [rooted trace symmetry sieve](docs/rooted-trace-symmetry-sieve-v0.md)
  proves that rooted automorphisms act freely on complete construction orders.
  The third comparison state `13709424605 = 5 * 2741884921` is not divisible
  by `2`, `3`, or `12`, so neither an unchanged `D6`-symmetric Flower-of-Life
  trace graph nor an indistinguishable two-face graph with a layer-swap
  involution can produce it as a complete build-order count. This eliminates
  two symmetry classes but does not select a constructor; phase/chirality,
  ordered coupling, an authorized quotient, and a different state extractor
  remain distinct unresolved alternatives.
- A second exact first-gate identity is recorded in the
  [event-pair quotient candidate](docs/event-pair-quotient-candidate-v0.md):
  `720 = C(39,2) - 21`, using the pinned 39 projection events and 21 band-pair
  relations. Its exterior-space and independent-constraint semantics are
  candidate assumptions. The unchanged operator returns `258819` from `720`
  and lifted gonol `1035277`, not `54837698421`, so it is `FALSIFIED` and emits
  no prediction.
- Successor testing resumed once for the frozen mechanics composition in
  [composed successor constructor v0](docs/composed-successor-constructor-v0.md).
  Result: `157 -> 629`, so this constructor/mechanics combination is
  `FALSIFIED`; the `2881 -> ?` gate was not run, no next prediction was frozen,
  and the interpolation control comparison was not reached.
- Successor testing is paused. The prior boundary pass is recorded in
  [gonol unresolved mechanics v0](docs/gonol-unresolved-mechanics-v0.md):
  `FALSIFIED = 8`, `UNRESOLVED = 3`, `SURVIVORS = 0`, no next prediction.
- Mechanic 1: [Public Gonol functional operations v0](docs/public-gonol-functional-operations-v0.md)
  defines executable carrier operations, composition, closure receipts,
  occurrence-addressed participation records, provenance, falsification
  conditions, and promotion evidence.
- Mechanic 2: [affinization coupling geometry v0](docs/affinization-coupling-geometry-v0.md)
  defines explicit ordered coupling, slot-sensitive degree, closed
  affinization receipts, rejected alternatives, falsification conditions, and
  promotion evidence.
- Mechanic 3: [recursive scale transition v0](docs/recursive-scale-transition-v0.md)
  defines closed-affinization-to-atomic-participant promotion with stable atomic
  identity, separate occurrence records, recoverable constituents,
  falsification conditions, and promotion evidence.
- PCEA key research remains gated until UCNS yields a surviving successor
  constructor from executable mechanics.
