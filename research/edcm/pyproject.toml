[build-system]
requires = ["setuptools>=77", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "edcm"
dynamic = ["version"]
description = "Provenance-bearing Energy–Dissonance Circuit Model measurement package"
readme = "README.md"
requires-python = ">=3.11"
license = "MPL-2.0"
license-files = ["LICENSE"]
authors = [{ name = "Erin Spencer" }]
keywords = ["measurement", "provenance", "transcript-analysis", "ucns", "metapat"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Typing :: Typed",
]
dependencies = []

[project.urls]
Homepage = "https://github.com/The-Interdependency/edcm"
Repository = "https://github.com/The-Interdependency/edcm"
Issues = "https://github.com/The-Interdependency/edcm/issues"

[project.optional-dependencies]
dev = ["build>=1.2", "pytest>=8.0", "twine>=5.0"]
metapat = [
  "metapat @ git+https://github.com/The-Interdependency/metapat.git@54bd394746426b37d5ca2e996071ace526fbdc9a",
]
ucns-profile = [
  "ucns @ git+https://github.com/The-Interdependency/ucns.git@a98c9e6c69804a8a08d0786b1d8b450bb2c49a97",
]
full-stack = [
  "metapat @ git+https://github.com/The-Interdependency/metapat.git@54bd394746426b37d5ca2e996071ace526fbdc9a",
  "ucns @ git+https://github.com/The-Interdependency/ucns.git@a98c9e6c69804a8a08d0786b1d8b450bb2c49a97",
]
# Historical v0.1-v0.4 joint experiments remain pinned to their evidence epoch.
# They are not the current word-gonol profile runtime.
ucns-experiments = [
  "ucns @ git+https://github.com/The-Interdependency/ucns.git@5331ae9a4cf7eddfa1de72b8caed28e2358cc0ed",
]
lexical-floor = [
  "PyYAML>=6,<7",
]

[tool.setuptools]
include-package-data = true

[tool.setuptools.packages.find]
where = ["."]
include = ["edcm*"]

[tool.setuptools.package-data]
edcm = ["py.typed", "measurement/canon/data/*.json", "corpora/data/*.json"]

[tool.setuptools.dynamic]
version = { attr = "edcm.__version__" }

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"
