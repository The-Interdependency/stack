name: english-gonol

on:
  push:
    paths:
      - "research/english-gonol/**"
      - "libs/ucns/**"
      - ".github/workflows/english-gonol.yml"
  pull_request:
    paths:
      - "research/english-gonol/**"
      - "libs/ucns/**"
      - ".github/workflows/english-gonol.yml"

permissions:
  contents: read

jobs:
  english-gonol-tests:
    runs-on: ubuntu-latest
    env:
      UCNS_SOURCE_ROOT: ${{ github.workspace }}/_deps/ucns-motion
    steps:
      - name: Checkout stack
        uses: actions/checkout@v7.0.1
        with:
          persist-credentials: false

      - name: Checkout pinned UCNS motion source
        uses: actions/checkout@v7.0.1
        with:
          repository: The-Interdependency/ucns
          ref: 1cf10c2df2541a332a77f2ed3feda0c6bef4abcc
          path: _deps/ucns-motion
          persist-credentials: false

      - name: Set up Python
        uses: actions/setup-python@v7.0.0
        with:
          python-version: "3.12"

      - name: Install pytest
        run: python -m pip install --disable-pip-version-check pytest

      - name: Verify pinned UCNS motion source
        run: test "$(git -C _deps/ucns-motion rev-parse HEAD)" = "1cf10c2df2541a332a77f2ed3feda0c6bef4abcc"

      - name: English Gonol construction tests
        working-directory: research/english-gonol
        run: python -m pytest -q tests
