# Canonical tools and libraries refresh — 2026-09-13

Stack start: `85d68392e298e2237d4240af88f711109d14f13e`. Mutation: source pins and their projections.
The [machine record](2026-09-13-tools.json) carries Git-tree identities, verification
results, generator hashes, original/current receipt hashes, and known failed tests.

## Source identities

| Repository | Previous commit | Refreshed upstream main commit |
|---|---|---|
| skill-lib | `fb3b53a7629f7f03ecf255167d52c13abef1a979` | `22c2c5702d14fb4b0faeb717777ecab2665770a1` |
| metapat | `34d954aa1e2092e615b03a180500f6b6977f501e` | `510e0171f4ecc6d1889e66bb66a8734c49c3b1fa` |
| ucns | `828c0b8bbcfc267efb5701da714191c1f73a81ff` | `31bb761e49307b04a7c7dd7c7c2059ea35fdc089` |
| edcm | `7951ca32ba0f2494dc68ff9b7f6a80151918a56d` | `ddc89a97ebbcf0a5863dad6e633b01b520e9bccf` |
| pcea | `91ffa8c7249dfb810ca64a0bbc500481c0bd12a9` | `75d2ea68e4a4a3256b324f039cb1712d196d323d` |
| ptcna | `97abdd1bbda61a68e0aac8595a32a3cb0ce73487` | `a06a049cd8722adaad32dbc9c36d9c87b0204236` |

All 882 imported Git blobs and executable/symlink modes match the owning
repositories exactly, including tracked dotfiles and removal of retired files.
The owning repositories' dirty working files were not imported.

The refreshed operational skill library includes `aggregate_org_rec.py`,
`org_ratio_compare.py`, the current MSDMD parser, and current build/evidence skills.
UCNS now carries its own MPL-2.0 license in this snapshot. This does not retroactively
change the licensing status of any older snapshot.

## Preserved boundaries and affected derivations

The existing research BASE records remain byte-identical. Alternate METAPAT, EDCM,
PCEA, PTCNA and English Gonol extraction identities are now explicit graph records,
alongside the previously separate UCNS research input. EPAC still consumes its
immutable v0.1.0 release and its own exact UCNS dependency. Neither authority nor
scientific standing transfers through this refresh.

The MSDMD generator changes from `sha256:bc845860efa0f2cde71f55ed21fbd107c31cbe992ce59542013d9d408e7ecf1f` to
`sha256:f25937594aaebb0088f3cbe2c2d107c43948b77b22cba1dedf4c4fcbc234f952`. A real collector fixture proved that the old receipt
is rejected under the new key, a new collection is independently reproduced and
accepted, and a repeated request is a no-op. The separately pinned fresh-making
doctrine remains byte-identical. No production state was changed or declared fresh.

The UCNS CANON file hash invalidated the event/trace receipt and its two consumers:
event-pair quotient and rooted-trace symmetry sieve. Those three receipts were
regenerated and independently replayed. Their results, standing, nonclaims and
unresolved boundaries are unchanged; only source/receipt hashes changed. The
paired machine record preserves their original hashes. Earlier bytes remain at
the Stack start commit above, for example:

```bash
git show 85d68392e298e2237d4240af88f711109d14f13e:research/ucns/receipts/event-trace-lift-candidate-v0.json
```

## Validation and usage

- Stack consistency and portfolio report validation pass.
- Imported source bytes/modes, skill-library editorial drift and vendored skill drift pass.
- Parser/report tools: 27 tests pass.
- Backend: 15 pass; 2 PostgreSQL tests are skipped without a disposable database.
- English Gonol: 62 pass with its exact historical lexical producer checkout.
- Python Gonol: 11 pass. AHBG runtime: 12 pass; the three-turn CLI smoke passes.
- Grok: 16 tests and 4 smoke scenarios pass; all 35 corpus scenarios survive and replay identically.
- UCNS/PCEA research: 113 pass and the same 23 fail in both the start and updated
  checkouts. The refresh adds no failed test identities after the three-receipt rebuild.

Run the structural and source-copy checks from the Stack root:

```bash
python3 tools/check_stack_consistency.py
python3 skill-lib/interdependent-work-graph/portfolio_plan.py \
  docs/work-graphs/repository-plan-report.json --output /tmp/stack-portfolio.json
python3 skill-lib/tools/check_consumer_drift.py . \
  --sha 22c2c5702d14fb4b0faeb717777ecab2665770a1 --strict-sha
# After committing, compare each imported subtree with the owning Git tree:
git rev-parse HEAD:libs/ucns
git -C ../ucns rev-parse 31bb761e49307b04a7c7dd7c7c2059ea35fdc089^{tree}
```

Use the same subtree comparison for the other five source rows (`HEAD:skill-lib`
for the operational snapshot). Compare full working-tree blobs and modes when
checking uncommitted changes; a Git tree alone describes committed content.

Run the receipt generators and their byte-replay tests:

```bash
PYTHONDONTWRITEBYTECODE=1 python3 research/ucns/event_trace_lift_candidate.py
PYTHONDONTWRITEBYTECODE=1 python3 research/ucns/event_pair_quotient_candidate.py
PYTHONDONTWRITEBYTECODE=1 python3 research/ucns/rooted_trace_symmetry_sieve.py
python3 -m pytest -q research/ucns/tests/test_event_trace_lift_candidate.py \
  research/ucns/tests/test_event_pair_quotient_candidate.py \
  research/ucns/tests/test_rooted_trace_symmetry_sieve.py
```

Run English tests from `research/english-gonol/` with `ENGLISH_GONOL_UCNS_ROOT`
pointing to a clean UCNS checkout at `d7c6f51304ed6c32d48badf63132bea6de8af497`.
Use Python 3.12 with pytest for the language suites. Re-run the historical research
comparison from each Stack checkout with:

```bash
python3 -m pytest -q research/ucns/tests research/pcea/tests --import-mode=importlib
```

## hmmm

The refreshed EDCM `docs/GONOL_LANGUAGE_BOUNDARY.md` still assigns language
construction to EDCM. Stack retains its declared English/Python construction
authority; this discrepancy is recorded in the manifest and must be corrected
at the EDCM source. The imported bytes are preserved, not edited as Stack canon.

Source-owned Markdown hard-break whitespace is retained in the exact snapshots;
the Stack-owned diff passes the whitespace check.

The 23 historical research failures predate this refresh: frozen receipt/source
hashes, source-history observations that scan moving local refs, and older PCEA
BASE assertions need their own research repair. They are not counted as passes.
The runtime suite emits an existing unclosed-pipe ResourceWarning in the unchanged
harness; the explicit backend ResourceWarning gate passes.

PostgreSQL integration and production derivation acceptance require their own
runtime evidence. After deployment, use the documented `fresh make-msdmd` command
to register current identities and verify affected collections before accepting
freshness. See [backend usage](../../backend/README.md).
